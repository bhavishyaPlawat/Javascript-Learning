/* 
write a program to calculate factorial of number using for loops
 */

let n = prompt("Enter a number to get Factorial");
let factorial = 1;
for (let i = 1; i <= n; i++) {
  factorial *= i;
}
console.log(factorial);
//ik this was easy but i was assigned the task so here it is
